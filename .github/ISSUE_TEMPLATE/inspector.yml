name: Apply as Inspector
description: Express interest in joining the Regeneration Credit as a inspector.
title: "[Inspector] "
labels: ["application", "role: inspector"]
body:
  - type: markdown
    attributes:
      value: |
        Inspectors visit regeneration areas and record their trees and species in a peer-to-peer verification, earning from the inspector pool for each inspection they complete. A minimum of three completed inspections makes an inspector eligible for pool rewards.

        **Everything you write below is public and permanent** (see the repository README).
        Membership is granted by an on-chain invitation from the community; there is no
        guaranteed timeline. Supporters do not need to apply — see
        <https://regenerationcredit.org/supporters>.
  - type: input
    id: name
    attributes:
      label: Name or handle
      description: A real name or a pseudonym — your choice.
    validations:
      required: true
  - type: input
    id: wallet
    attributes:
      label: Wallet address
      description: The address that would receive the on-chain invitation.
      placeholder: "0x0000000000000000000000000000000000000000"
    validations:
      required: true
  - type: textarea
    id: contribution
    attributes:
      label: How can you contribute as a inspector?
      description: What would you work on? Links to past work or a short plan help the community decide.
    validations:
      required: true
  - type: input
    id: contact
    attributes:
      label: Contact (optional)
      description: Discord handle, email or anything else, if you want the community to be able to reach you off GitHub.
    validations:
      required: false
  - type: checkboxes
    id: consent
    attributes:
      label: Consent
      options:
        - label: I understand this application and everything in it — including my wallet address — is public and permanent.
          required: true
